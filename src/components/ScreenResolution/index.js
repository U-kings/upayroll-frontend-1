import React from "react";
import {
  ScreenResolutionContainer,
  ScreenResolutionContent,
} from "../../styles/library";

const ScreenResolution = () => {
  return (
    <ScreenResolutionContainer>
      <ScreenResolutionContent>
        <h1>Please visit this site on a larger screen size.</h1>
      </ScreenResolutionContent>
    </ScreenResolutionContainer>
  );
};

export default ScreenResolution;
